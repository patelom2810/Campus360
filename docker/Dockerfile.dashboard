FROM python:3.11-alpine

WORKDIR /usr/share/campus360/dashboard

# Copy static dashboard files (HTML, CSS, JS)
COPY src/dashboard/ .

EXPOSE 8501

# Lightweight static HTTP server on port 8501
CMD ["python", "-m", "http.server", "8501", "--bind", "0.0.0.0"]
